{
  "name": "nb-lao-backend",
  "version": "0.1.0",
  "private": true,
  "description": "NB LAO E-Commerce backend foundation",
  "scripts": {
    "start": "node backend/server.js",
    "test": "node --test tests/phase2.test.js",
    "db:push": "npx prisma db push",
    "db:studio": "npx prisma studio",
    "db:backup": "node scripts/backup-db.js",
    "db:restore": "node scripts/restore-db.js",
    "db:cleanup-images": "node scripts/cleanup-images.js",
    "db:cleanup-dry-run": "node scripts/cleanup-images.js --dry-run"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.11.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
